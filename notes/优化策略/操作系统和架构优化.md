- **Introduce Industrial-Grade Architecture:** Abandon writing a bunch of loose Python scripts, and adopt **ROS/ROS2 (Robot Operating System)** to manage nodes. Sensor reading, decision inference, and low-level motor control should run as independent Nodes, communicating via Topics.
    
- **Multi-modal or End-to-End:** Discard simple ultrasonic sensors and do "end-to-end" visual RL. Input the raw RGB images (Pixels) from the Raspberry Pi camera, extract features through a Convolutional Neural Network (CNN), and directly output control commands (Actions).
    
- **Paper Output:** Besides the above contents, it will also include a strict Ablation Study—for instance, comparing the success rate difference in real-world venues between "with domain randomization" and "without domain randomization" to prove your improvements are genuinely effective.